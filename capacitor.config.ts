import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'tk.jralphw.mkozi',
  appName: 'mkozi',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
